# Mandala Map · Fraktal49

Generated: 2025-10-08T12:00:00Z
Repository: unified-mandala

## Reference Docs

- `docs/roadmap/v1.0-stabilization-playbook.md`
- `docs/roadmap/v1.0-stabilization-playbook.yaml`

## Legend

### Status Codes

- **stable** – Part of hardened release/CI flows.
- **active** – Maintained and in current use; expect ongoing iteration.
- **experimental** – Prototype or research area; coverage may be partial.
- **in-progress** – Partially implemented; additional engineering required.
- **stub** – Placeholder or type stub awaiting production implementation.
- **generated** – Derived artifacts or caches regenerated by scripts.
- **backlog** – Planning/backlog data to feed repositorypflege automation.
- **system** – Infrastructure-managed assets (no manual editing).

### Categories

- **core-runtime** · Core Runtime & Workspaces — TypeScript/Python/Go workspaces that ship product code. (stewards: DevX Guild, ReleaseOps Circle)
- **agents** · Agent Complexes & AI Programs — Autonomous/assisted agent systems and orchestrators. (stewards: CodexAuditAgent, EvolverGPT)
- **data-intel** · Data, Pipelines & Analytics — Datasets, ingestion utilities, generated indices. (stewards: Climate Lab, Resonance Lab)
- **ci-infra** · CI/CD & Infrastructure — Workflows, deployment manifests, runtime parity assets. (stewards: Codex CoreOps, SyncRunner)
- **governance** · Governance, Policy & Narrative — Policy, context, and strategic documentation. (stewards: AI Governance Council, PactDepthGatekeeper)
- **observability** · Observability & Telemetry — Monitoring dashboards, metrics wiring, RUM. (stewards: TelemetryOps, GenesisAeonNavigator)
- **automation** · Automation & Repo Maintenance — Scripts that keep the codex synchronized and consistent. (stewards: Repositorypflege Collective, QualityAssuranceAgent)
- **testing** · Testing & QA — Unit/integration/regression assets. (stewards: QualityAssuranceAgent)
- **backlog** · Backlog & Planning Artifacts — Structured to-do manifests and fractal inputs. (stewards: Codex)
- **support** · Support & System Assets — Scaffolding such as stubs, keys, vendored modules. (stewards: Dev Infrastructure)
- **research** · Research & Prototyping — Exploratory demos, experiments and simulations. (stewards: EvolverGPT, Research Agents)

## Support & System Assets (`support`)

- `.git/` — **system** Git metadata
  - Internal Git control files; managed by tooling and excluded from builds.
  - Notes: No manual edits; included for completeness of the tree.

- `keys/` — **stub** Keys placeholder
  - Empty directory reserved for secrets/keys (kept out of version control).
  - Notes: Populate via deployment automation only; keep empty in repo.

- `node_modules/` — **generated** Node modules
  - Installed dependencies for pnpm workspace.
  - Notes: Regenerate via pnpm install; do not commit manual edits.

- `pyright-stubs/` — **stable** Pyright stubs
  - Vendored type stubs (numpy, xarray) supporting strict pyright checks.
  - Notes: Update when upstream versions change; referenced by pyrightconfig.json.

  - `stubs/` — **stub** Python stub packages
    - Placeholder modules for kan_network, liquid_time_constant_network, torch_geometric, etc.
    - Notes:
      - Roadmap in docs/roadmap/stub-replacement-roadmap.(md|yaml) beschreibt Ablösung und Keys-Governance.
      - Bis Phase B abgeschlossen ist, stubs/ nur als Typ-Fallback nutzen; keys/ bleibt automation-only.
    - Links: [Stub replacement roadmap](docs/roadmap/stub-replacement-roadmap.md)

- `typings/` — **stable** Typing shims
  - Third-party typing shims (aiohttp, fastapi, numpy, pandas, etc.) for cross-language tooling.
  - Notes: Used by pyright and type checkers for mixed-language stack.

## CI/CD & Infrastructure (`ci-infra`)

- `.github/` — **active** GitHub workflows & templates
  - Workflow definitions (core, nightly, experimental) and community health files.
  - Notes: Audit instructions encoded as jobs; isolate non-actionable tasks to stop false CI failures.; Core/extended workflows documented in docs/roadmap/v1.0-stabilization-playbook.md.; Sigillin validator is required on main; mandala-map workflow uploads artifacts without blocking CI.; Repo Sanity builds the UI dist and waits on apps/ui/dist/index.html before running map/audit checks to avoid race conditions.
  - Links: [CI Core](.github/workflows/ci.core.yml), [Nightly Mirror](.github/workflows/ci.nightly.yml), [Sigillin validate](.github/workflows/sigillin-validate.yml), [Mandala map job](.github/workflows/mandala-map.yml)

- `.husky/` — **active** Git hooks
  - Pre-commit automation enforcing lint-staged and formatting policies.
  - Notes: Keep hooks aligned with pnpm scripts and policy suite.
  - Links: [Pre-commit](.husky/pre-commit)

- `aws/` — **active** AWS deployment stubs
  - Lambda@Edge and related infrastructure definitions aligning with deployment ready stacks.
  - Notes: Coordinate changes with deployment/ and infrastructure/ to maintain parity.
  - Links: [lambda-edge](aws/lambda-edge)

- `charts/` — **active** Helm charts
  - Helm configuration (ghostshell) for Kubernetes rollouts.
  - Notes: Ensure charts stay aligned with k8s/ manifests and Compose profiles.
  - Links: [ghostshell chart](charts/ghostshell)

- `ci/` — **active** CI support scripts
  - Reusable CI scripts/tests (agent.yml, sigillin validators, OTEL wiring).
  - Notes: Review shell helpers to make sure instructions are not executed as failing jobs.; See docs/roadmap/v1.0-stabilization-playbook.yaml checkpoints for CI ownership.
  - Links: [agent workflow](ci/agent.yml)

- `codexbuild/` — **active** Codex build manifests
  - Build definitions for aeon, sharedream-interface, unified-mandala packaging.
  - Notes: Connect to release automation and ensure versions align with pnpm workspace state.
  - Links: [unified-mandala build](codexbuild/unified-mandala.build.yaml)

- `deployment/` — **active** Deployment blueprints
  - Deployment manifests (fractal-ci, keda, pantheon, pyramid-ui) bridging to ops stacks.
  - Notes: Sync with k8s/ and docker-compose profiles when updating services.

- `infrastructure/` — **active** Infrastructure modules
  - Infrastructure stacks (nats, otel, protodeploy) for service deployment.
  - Notes: Ensure Compose/K8s manifests stay aligned with infrastructure definitions.

- `k8s/` — **active** Kubernetes manifests
  - K8s deployment specs (deploy-rag-api.yaml, keda-scalers.yaml).
  - Notes: Review alongside deployment/ and charts/ during ops changes.

- `localhost-offline/` — **active** Offline stack
  - Offline docker-compose profile and docs for local environment parity.
  - Notes: Use with pnpm start:light and offline smoke tests.

## Automation & Repo Maintenance (`automation`)

- `.registry/` — **active** Plugin registry cache
  - Static registry for published Mandala plugins consumed by public/plugins.html.
  - Notes: Update via scripts when plugin definitions in plugins/ change.
  - Links: [plugins.json](.registry/plugins.json)

- `codex-sync/` — **active** Codex sync scripts
  - Node automation (answer-suggestions.js) to propagate codex updates across repos.
  - Notes: Run via pnpm scripts when synchronizing codex states.
  - Links: [answer-suggestions](codex-sync/answer-suggestions.js)

- `fraktalrun/` — **active** Fraktalrun pipelines
  - Manifests describing fractal run orchestration and task schemas.
  - Notes: MandalaMap extends existing repo mapping inside fractal pipelines.
  - Links: [pipeline](fraktalrun/pipeline.yaml)

- `integration/` — **active** Integration helpers
  - Integration scripts and packs bridging external systems.
  - Notes: Use integration/apply.mjs when syncing packages across envs.

- `processes/` — **active** Process automation
  - Scripts seeding models, universe trees, and utopie adapters.
  - Notes: Review when adjusting dataset bootstrapping or storytelling flows.

- `repositorypflege/` — **active** Repository maintenance
  - Scripts and tests for repo health (advanced progress summaries, todo parsing).
  - Notes: Use to regenerate advancedprogress.json and ToDo trackers.

- `runners/` — **active** Runner scripts
  - Mandala research runner utilities and subprocess bridges (TypeScript).
  - Notes: Support CLI/automation flows for research tasks.

- `scripts/` — **active** Automation scripts
  - Extensive Node/TS automation: dev servers, exports, QA, smoke tests, repo map, etc.
  - Notes: Dist-first convention via scripts/run-dist.mjs; Sigillin-Fix-CLI (`scripts/fix-sigillin.ts`) ergänzt Bridge-Tooling; Windows-Parität via `scripts/setup-dev-env.ps1` + `scripts/run-powershell.mjs` dokumentiert; PowerShell-Setup exportiert Installationsstatus nach `out/setup/install-state.json`.
  - Links: [run-dist](scripts/run-dist.mjs), [dev-services](scripts/dev-services.mjs), [sigil-fix](scripts/fix-sigillin.ts), [setup-dev-env.ps1](scripts/setup-dev-env.ps1), [run-powershell](scripts/run-powershell.mjs)

- `tasks/` — **active** Task manifests
  - Codex task definitions and sigillin skeleton specs driving automation flows.
  - Notes: Keep synchronized with codexfeedback hooks and fractal analysis.

- `tools/` — **active** Tooling
  - Auxiliary tools (fraktal lab CLI, governance checks, Kyverno runner).
  - Notes: Used by policy:check and governance automation; Windows helper builds laufen via `pnpm build:windows-tools` → `scripts/run-powershell.mjs`.

## Agent Complexes & AI Programs (`agents`)

- `GenesisAeonAdvancedAi/` — **active** Genesis Aeon Advanced AI
  - Python agent stack with CLI, adaptive thresholding, and visualization modules.
  - Notes: Extensive pytest coverage (test\_\*.py) validates archetype and sigil tooling.
  - Links: [Masterplan](GenesisAeonAdvancedAi/masterplan.md)

- `GenesisAeonZIPMEM/` — **active** Genesis Aeon ZIPMEM
  - Self-organizing memory orchestration with commitMemory utilities and agent pipelines.
  - Notes: Contains fractal agent scripts and tests ensuring memory consistency.
  - Links: [ZIPMEM manifest](GenesisAeonZIPMEM/ZIPMEM_manifest.yaml)

- `agents/` — **active** Python agent modules
  - Domain-specific agent implementations (art, contrastive training, defensive shield, etc.).
  - Notes: Integrated with scripts/agents runner pipeline and policy checks.
  - Links: [Runner](scripts/agents/runner.ts)

- `go-agent/` — **active** Go agent runtime
  - Go-based agent implementation with cmd/internal packages and CI harness.
  - Notes: Ensure Go tooling aligns with Node/TS orchestrators for metrics integration.
  - Links: [go.mod](go-agent/go.mod)

- `go-bridge/` — **active** Go bridge
  - Go command bridge providing API adapters between services and agents.
  - Notes: Shares patterns with go-agent; maintain parity in README and scripts.
  - Links: [README](go-bridge/README.md)

- `orchestrator/` — **active** Python orchestrator
  - Newsbot orchestrator and related publishing tests.
  - Notes: Ensure parity with unifiedmandala-orchestrator workspace.

- `unifiedmandala-neural/` — **experimental** Unified Mandala Neural
  - Neural experiments (GNN/HGNN, KAN physics, liquid nets, SNN haptic) with docker-compose support.
  - Notes: Coordinate GPU/compute requirements before integrating into CI.

- `unifiedmandala-orchestrator/` — **experimental** Unified Mandala Orchestrator
  - Orchestrator workspace (nets, orchestrator, src) for coordinating agent nets.
  - Notes: Ensure compatibility with top-level orchestrator/ Python modules.

## Backlog & Planning Artifacts (`backlog`)

- `advancedToDo_parts/` — **backlog** Advanced ToDo fragments
  - Chunked advanced ToDo datasets (.json/.yaml) referenced by repositorypflege automation.
  - Notes: Use repositorypflege/advanced-todo-report.js to rebuild consolidated views.

## Data, Pipelines & Analytics (`data-intel`)

- `analysis/` — **generated** Analytics outputs
  - Generated datasets (conversation filters, repo-map JSON/CSV, audits).
  - Notes:
    - scripts/repo-map.ts writes analysis/repo-map.json and CSV; keep artifacts in sync with MandalaMap.\*
    - scripts/generate-trikaya-dashboard.mjs produziert analysis/trikaya-dashboard.(json\|md\|yaml) für CREP/Trikāya-Governance.
  - Links: [Repo map script](scripts/repo-map.ts) · [Trikāya dashboard script](scripts/generate-trikaya-dashboard.mjs)

- `data/` — **active** Data lake
  - Raw/processed datasets (agents, MRV, personhood, sigils, simulations).
  - Notes: Ensure adapters (src/adapters) consume curated data snapshots.

- `ingest/` — **active** Ingestion scripts
  - Python ingestion utilities (e.g., merge_epi_abundance.py).
  - Notes: Coordinate with pipelines/universe_pulse.yaml and data/raw snapshots.

- `out/` — **generated** Generated outputs
  - Build artifacts such as sigillin_index.json and validation error exports.
  - Notes: Cleared/rebuilt by validation scripts (pnpm validate:sigillins, etc.).

- `pipelines/` — **active** Pipeline manifests
  - Pipelines (e.g., universe_pulse.yaml) orchestrating data/analysis flows.
  - Notes: Reference in docs/roadmap and release drills when scheduling pipeline runs.

## Core Runtime & Workspaces (`core-runtime`)

- `apps/` — **active** Workspace applications
  - React/Vite frontends (mandala-ui, sharedream-interface, pyramid-ui, mobile prototypes).
  - Notes: Primary UI served from apps/ui (workspace name mandala-ui).; Dev entrypoints: pnpm -F mandala-ui dev | pnpm -F mandala-ui build.
  - Links: [apps/ui package.json](apps/ui/package.json)

- `packages/` — **active** Workspace packages
  - Primary TypeScript/Node packages grouped by domains (aeon-_, unifiedmandala-_, etc.).
  - Notes: Cover core libs, agents, domain modules, UI components, CLI tools.; Validated via pnpm test:ts:ci and pnpm lint commands.

- `public/` — **active** Public assets
  - Static assets (plugins.html, js) for the Mandala UI.
  - Notes: Keep plugin listing consistent with plugins/ definitions.

- `services/` — **active** Service implementations
  - Node/TS services (ghost-shell, chat proxy, codex memory kernel) with tests.
  - Notes: Run via pnpm dev:services / pnpm start:services as documented in release drills.

- `src/` — **active** Python source
  - Python modules (adapters, metrics, membrane, middleware) powering data pipelines.
  - Notes: Validated via pnpm test:py and npx pyright; keep ERA5/OISST adapters green.

- `types/` — **stable** Type definitions
  - TypeScript declaration overrides (automerge, geotiff, node, pdf-parse, stream-json).
  - Notes: Support workspace TypeScript builds; keep in sync with dependencies.

## Governance, Policy & Narrative (`governance`)

- `codex/` — **active** Codex governance manifests
  - Codex strategy, phase planning, sync manifests, GPT role definitions.
  - Notes: Keep in sync with codexfeedback.\* trackers and fractal plans.
  - Links: [codex-roadmap.yaml](codex/codex-roadmap.yaml)

- `config/` — **active** Configuration bundles
  - Cross-domain configs (agent-services.yaml, emergence-detector.yaml, memory jobs).
  - Notes: Reference from services/scripts; update docs when schema changes.
  - Links: [agent-services](config/agent-services.yaml)

- `docs/` — **active** Documentation
  - Core documentation including stabilization playbooks, onboarding, agents, governance.
  - Notes: MandalaMap references docs/roadmap/v1.0-stabilization-playbook.\* für CI/Release Kontext und die Stub-Roadmap für Support-Migrationen.
  - Links: [v1.0 Playbook](docs/roadmap/v1.0-stabilization-playbook.md) · [Stub roadmap](docs/roadmap/stub-replacement-roadmap.md)

- `governance/` — **active** Governance policies
  - Governance policy documents and nested policy definitions.
  - Notes: Coordinate with policies/ and AI_POLICY.\* updates.

- `manifest/` — **active** Manifest documents
  - Narrative manifests such as blackbox-manifest.md and crep-open-ethik.md.
  - Notes: Align with worldview/ and codex strategy updates.

- `plugins/` — **active** Plugin manifests
  - YAML plugin descriptors consumed by UI and automation flows.
  - Notes: Coordinate updates with public/plugins.html and .registry/plugins.json.

- `policies/` — **active** Policy suite
  - OPA/Kyverno/Guardrail policy definitions for governance enforcement.
  - Notes: Used by pnpm policy:check and CI policy suite.
  - Links: [merge guardrails](policies/merge-guardrails.yaml)

- `prompts/` — **active** Prompt library
  - Curated prompt templates (emergence scan, schema critic).
  - Notes: Support agent prompts; align with Evolution/Emergence flows.

- `schemas/` — **active** Schema definitions
  - JSON schemas (eventbus.message.schema.json) referenced by validation scripts.
  - Notes: Keep versioned with tools/kyverno-dry-run.mjs expectations.

- `sigils/` — **active** Sigillin bridges & archives
  - Machine-readable Sigillin briefs (JSON/YAML) and bridging bundles für AI-Orchestrierung.
  - Notes: Generated via scripts/scaffold-interai-bridges.mjs; validate via pnpm validate:sigillins.; Validator scope targets sigils/bridges with registry/archives skipped and agent overlays adding minimal semantics for legacy entries.

- `sigils/bridges/bridges.index.yaml` — **active** Bridge registry index
  - Registry listing all inter-AI bridge sigillins for discovery tooling.
  - Notes: Generated alongside bridge scaffolds; keep in sync with pnpm validate:sigillins outputs.; Consumed by map:mandala validation and upcoming CREP/Trikāya dashboards.
  - Links: [bridges/](sigils/bridges)

- `worldview/` — **active** Worldview manifests
  - Mandala worldview YAML encoding narrative/ethical positioning.
  - Notes: Align updates with manifest/ and AI policy documentation.

## Testing & QA (`testing`)

- `cypress/` — **active** Cypress end-to-end tests
  - Cypress config and specs for UI smoke/regression tests.
  - Notes: Coordinate with pnpm cy:run and vitest offline settings.
  - Links: [Config](cypress.config.ts)

- `fixtures/` — **active** Fixtures
  - Test fixtures across adapters, sigils, datasets for deterministic testing.
  - Notes: Keep in sync with tests/ and src/adapters requirements.

- `tests/` — **active** Test suites
  - Vitest/Jest/Pytest assets spanning adapters, agents, ethics, event bus, etc.
  - Notes: Core CI executes via pnpm test:ts:ci and pnpm test:py.

- `validation/` — **active** Validation utilities
  - Sanity check scripts (sanity_checks.py) for dataset validation.
  - Notes: Integrate into data ingestion QA loops.

## Research & Prototyping (`research`)

- `demos/` — **experimental** Demos
  - Prototype demos such as universe_pulse_e2e.ts for showcasing pipelines.
  - Notes: Validate compatibility with current package APIs before reuse.

- `examples/` — **experimental** Examples
  - Sample integrations (e.g., go-agent) demonstrating API usage.
  - Notes: Use as references when onboarding new service stacks.

- `experiments/` — **experimental** Experiments
  - Research prototypes (A/B tests, emergence studies, self tuning).
  - Notes: Results feed into Resonance and Climate labs; validate before productionizing.

- `sims/` — **experimental** Python simulations
  - Fractal universe tree simulations and optimism studies (Python).
  - Notes: Feed simulation outputs into research packages before productionizing.

- `simulations/` — **experimental** Simulation configs
  - Simulation definitions (YAML, Python) for universe modelling and wave studies.
  - Notes: Coordinate with sims/ when running end-to-end experiments.

## Observability & Telemetry (`observability`)

- `grafana/` — **active** Grafana dashboards
  - Dashboard JSON for monitoring; complements observability/prometheus.yml.
  - Notes: Use docker-compose monitoring profile to validate dashboards.

- `observability/` — **active** Observability package
  - Prometheus config, golden signals scripts, Grafana integrations.
  - Notes: Part of release drills described in docs/roadmap/v1.0-stabilization-playbook.md.

- `rum/` — **in-progress** Real User Monitoring
  - RUM instrumentation (otel-web.js) awaiting integration with observability stack.
  - Notes: Connect with observability/ golden signals once ready.

## Follow-ups

- **Separate instructional jobs from actionable CI** (done) — Mandala-map workflow runs continue-on-error and publishes artifacts; instructional notes no longer block required checks. · Related: .github/, ci/
- **Stub replacement roadmap** (in-progress) — Roadmap dokumentiert in docs/roadmap/stub-replacement-roadmap.(md|yaml); Umsetzungsschritte für stubs/ und keys/ eingeplant. · Related: stubs/, keys/
- **MandalaMap upkeep** (ongoing) — Update MandalaMap.\* after each fractal iteration to capture structural changes. · Related: MandalaMap.yaml, MandalaMap.json, MandalaMap.md
