name: ci.core

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]
  push:
    branches: [main]

jobs:
  decide:
    name: Detect changes and CI level
    runs-on: ubuntu-latest
    outputs:
      level: ${{ steps.level.outputs.level }}
      docs_changed: ${{ steps.filter.outputs.docs }}
      maps_changed: ${{ steps.filter.outputs.maps }}
      code_changed: ${{ steps.filter.outputs.code }}
      labels: ${{ steps.labels.outputs.names }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: filter
        uses: dorny/paths-filter@v3
        with:
          list-files: shell
          filters: |
            docs:
              - 'docs/**'
              - '**/*.md'
              - '**/*.mdx'
              - 'codexfeedback*'
              - 'MandalaMap.*'
            maps:
              - 'docs/maps/**'
              - 'MandalaMap.*'
            code:
              - '.github/**'
              - 'apps/**'
              - 'packages/**'
              - 'services/**'
              - 'scripts/**'
              - 'src/**'
              - 'ci/**'
              - 'cypress/**'
              - 'integration/**'
              - 'orchestrator/**'
              - 'unifiedmandala-*/**'
              - 'pipelines/**'
              - 'tests/**'
              - 'tools/**'
              - '!**/*.md'
              - '!docs/**'
      - id: labels
        shell: bash
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json, os
          with open(os.environ.get('GITHUB_EVENT_PATH', ''), 'r', encoding='utf-8') as fh:
              data = json.load(fh)
          labels = [label['name'] for label in data.get('pull_request', {}).get('labels', [])]
          print(f"names={json.dumps(labels)}")
          PY
      - id: level
        shell: bash
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json, os
          with open(os.environ.get('GITHUB_EVENT_PATH', ''), 'r', encoding='utf-8') as fh:
              data = json.load(fh)
          labels = {label['name'] for label in data.get('pull_request', {}).get('labels', [])}
          level = 'core'
          if 'ci:extended' in labels:
              level = 'extended'
          if 'ci:runtime' in labels:
              level = 'runtime'
          print(f'level={level}')
          PY

  type_and_tests:
    name: Type, lint, build & coverage gate
    runs-on: ubuntu-latest
    needs: [decide]
    if: >-
      ${{
        github.event_name == 'push' ||
        needs.decide.outputs.code_changed == 'true' ||
        needs.decide.outputs.docs_changed == 'true' ||
        contains(fromJSON(needs.decide.outputs.labels), 'ci:extended')
      }}
    env:
      OFFLINE: '1'
      LOW_MEM: '1'
      VITE_LOW_MEM: 'on'
      NODE_OPTIONS: '--max-old-space-size=2048'
      CI: 'true'
    steps:
      - uses: actions/checkout@v4
      - name: Typecheck
        uses: ./.github/actions/run-pnpm-task
        with:
          task: typecheck
      - name: Lint
        uses: ./.github/actions/run-pnpm-task
        with:
          task: lint:eslint
      - name: Format check
        uses: ./.github/actions/run-pnpm-task
        with:
          task: lint:format
      - name: Build API workspace
        uses: ./.github/actions/run-pnpm-task
        with:
          task: -F @unified-mandala/api build
      - name: Unit tests with coverage
        uses: ./.github/actions/run-pnpm-task
        with:
          task: test:unit:coverage
      - name: Upload coverage artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: coverage-vitest
          path: |
            coverage
            coverage-final.json
          if-no-files-found: ignore
      - name: Schema validation
        uses: ./.github/actions/run-pnpm-task
        with:
          task: schema:validate
      - name: Map validation
        uses: ./.github/actions/run-pnpm-task
        with:
          task: maps:validate
      - name: Repository sanity
        uses: ./.github/actions/run-pnpm-task
        with:
          task: sanity
      - name: Policy suite
        uses: ./.github/actions/run-pnpm-task
        with:
          task: policy:check

  opa_policy:
    name: OPA Policy & Coverage
    runs-on: ubuntu-latest
    needs: [decide]
    timeout-minutes: 15
    if: >-
      ${{
        github.event_name == 'push' ||
        needs.decide.outputs.code_changed == 'true' ||
        needs.decide.outputs.docs_changed == 'true' ||
        contains(fromJSON(needs.decide.outputs.labels), 'ci:extended')
      }}
    env:
      OPA_MIN_COVERAGE: ${{ github.ref == 'refs/heads/main' && '0.9' || '0.85' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Enable corepack & install
        run: |
          corepack enable
          pnpm i --frozen-lockfile

      - name: OPA format
        run: pnpm -w opa:fmt

      - name: OPA lint
        run: pnpm -w opa:lint

      - name: Run OPA tests with coverage
        run: pnpm -w opa:cover

      - name: Upload coverage JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opa-coverage-json
          path: dist/opa/coverage.json
          if-no-files-found: warn

      - name: Summarize coverage
        if: always()
        run: |
          node -e "const fs=require('fs');const p='dist/opa/coverage.json';if(!fs.existsSync(p)){process.exit(0);}const r=JSON.parse(fs.readFileSync(p,'utf8'));const f=r.coverage?.files||{};let c=0,t=0;for(const v of Object.values(f)){const a=(v.covered||[]).length,b=(v.not_covered||[]).length;c+=a;t+=a+b}const pct=t?((c/t)*100).toFixed(2):'0.00';fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY,`### OPA Coverage\n\n**${pct}%** (${c}/${t})\n`);"

      - name: OPA build + sign + verify
        env:
          OPA_SIGN_ENABLE: '1'
          OPA_SIGN_GPG_BIN: gpg
          OPA_SIGN_GPG_KEY: ${{ secrets.MANDALA_GPG_KEY_ID }}
        run: pnpm -w opa:bundle:sign

      - name: Upload OPA bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: mandala-ethics-bundle
          path: |
            dist/opa/mandala-ethics-bundle.tar.gz
            dist/opa/mandala-ethics-bundle.tar.gz.sha256
            dist/opa/mandala-ethics-bundle.tar.gz.asc

  ai_commit_guard:
    name: AI commit guard
    runs-on: ubuntu-latest
    needs: [decide]
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - uses: actions/checkout@v4
      - name: Guard runtime vs repo writes
        run: node scripts/ci/ai-commit-guard.mjs

  advisory_suite:
    name: Advisory suite (soft fail)
    runs-on: ubuntu-latest
    needs: [decide]
    if: >-
      ${{
        github.event_name == 'push' ||
        contains(fromJSON(needs.decide.outputs.labels), 'ci:advisory')
      }}
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Advisory bundle (check:ci)
        uses: ./.github/actions/run-pnpm-task
        with:
          task: check:ci

  repomap_when_needed:
    name: Repomap (on demand)
    runs-on: ubuntu-latest
    needs: [decide]
    if: >-
      ${{
        needs.decide.outputs.maps_changed == 'true' ||
        contains(fromJSON(needs.decide.outputs.labels), 'run:repomap') ||
        contains(fromJSON(needs.decide.outputs.labels), 'ci:extended')
      }}
    steps:
      - uses: actions/checkout@v4
      - name: Build repomap
        uses: ./.github/actions/run-pnpm-task
        with:
          task: repomap:build
      - name: Validate repomap
        uses: ./.github/actions/run-pnpm-task
        with:
          task: repomap:validate
