commit ed950d564bdbf1761c20200fe32cb96d9ae468cc
Author: Codex <codex@openai.com>
Date:   Thu Jun 26 14:32:03 2025 +0000

    feat(ai): add trend analysis utilities

diff --git a/GenesisAeonAdvancedAi/__init__.py b/GenesisAeonAdvancedAi/__init__.py
index b0204cb..3d0b8eb 100644
--- a/GenesisAeonAdvancedAi/__init__.py
+++ b/GenesisAeonAdvancedAi/__init__.py
@@ -14,6 +14,7 @@ from .memory_store import (
     summarize_memory,
     summarize_entries,
     tail_results,
+    trend_metric,
 )
 
 __all__ = [
@@ -34,5 +35,6 @@ __all__ = [
     "summarize_memory",
     "summarize_entries",
     "tail_results",
+    "trend_metric",
 ]
 
diff --git a/GenesisAeonAdvancedAi/aeon_cli.py b/GenesisAeonAdvancedAi/aeon_cli.py
index 3a6ebc2..007c0ab 100644
--- a/GenesisAeonAdvancedAi/aeon_cli.py
+++ b/GenesisAeonAdvancedAi/aeon_cli.py
@@ -20,8 +20,13 @@ from .aeon_processor import (
     advanced_crep_eval,
 )
 from .performance_monitor import monitor_performance
-from .memory_store import store_result, load_results, summarize_memory
-from .memory_store import tail_results
+from .memory_store import (
+    store_result,
+    load_results,
+    summarize_memory,
+    tail_results,
+    trend_metric,
+)
 from .sigil_loader import load_sigil, load_start_sigil
 from .trikaya import trikaya_state
 from .archetype_tools import get_symbol
@@ -70,6 +75,17 @@ def main(argv: List[str] | None = None) -> None:
         metavar="N",
         help="Display the last N memory entries (requires --memory)",
     )
+    parser.add_argument(
+        "--trend-key",
+        help="Compute average change for given numeric key in memory and exit",
+    )
+    parser.add_argument(
+        "--trend-window",
+        type=int,
+        default=5,
+        metavar="N",
+        help="Number of recent entries to consider for --trend-key",
+    )
     parser.add_argument(
         "--sigil",
         type=Path,
@@ -138,6 +154,13 @@ def main(argv: List[str] | None = None) -> None:
         print(json.dumps(entries, indent=2))
         return
 
+    if args.trend_key:
+        if not args.memory:
+            parser.error("--trend-key requires --memory")
+        trend = trend_metric(args.memory, args.trend_key, args.trend_window)
+        print(json.dumps({"trend": trend}, indent=2))
+        return
+
     values = list(args.values)
     if args.input:
         text = args.input.read_text().strip()
diff --git a/GenesisAeonAdvancedAi/feedback.md b/GenesisAeonAdvancedAi/feedback.md
index 003d469..9f30033 100644
--- a/GenesisAeonAdvancedAi/feedback.md
+++ b/GenesisAeonAdvancedAi/feedback.md
@@ -13,3 +13,5 @@ Erweiterung: Aeon CLI unterstuetzt jetzt das Flag `--graph`, um einen Fraktal-Fe
 - Fraktal-Graph-Nodes enthalten jetzt Trikaya-Zustand.
 - Tail utility `tail_results` added with corresponding `--tail` CLI flag and tests.
 - Aeon CLI supports `--archetype-context` to display matching archetype symbols.
+- Trend analysis via `trend_metric` added. CLI option `--trend-key` computes
+  average change for a numeric field.
diff --git a/GenesisAeonAdvancedAi/memory_store.py b/GenesisAeonAdvancedAi/memory_store.py
index 102f910..9bdaf01 100644
--- a/GenesisAeonAdvancedAi/memory_store.py
+++ b/GenesisAeonAdvancedAi/memory_store.py
@@ -67,3 +67,23 @@ def tail_results(path: Path, n: int = 10) -> List[Dict[str, Any]]:
         return []
     return results[-n:]
 
+
+def trend_metric(path: Path, key: str, n: int = 5) -> float | None:
+    """Return average stepwise change for ``key`` in the last ``n`` entries.
+
+    Parameters
+    ----------
+    path:
+        Path to the JSON memory file.
+    key:
+        Numeric field to analyze.
+    n:
+        Number of recent entries to evaluate.
+    """
+    entries = tail_results(path, n)
+    values = [e.get(key) for e in entries if isinstance(e.get(key), (int, float))]
+    if len(values) < 2:
+        return None
+    diffs = [values[i] - values[i - 1] for i in range(1, len(values))]
+    return sum(diffs) / len(diffs)
+
diff --git a/GenesisAeonAdvancedAi/test_cli.py b/GenesisAeonAdvancedAi/test_cli.py
index 44c6111..43744ca 100644
--- a/GenesisAeonAdvancedAi/test_cli.py
+++ b/GenesisAeonAdvancedAi/test_cli.py
@@ -241,3 +241,34 @@ class TestAeonCLI(unittest.TestCase):
             self.assertIn("archetype", data)
             self.assertEqual(data["archetype"].get("symbol"), "X")
 
+    def test_trend_flag(self):
+        with tempfile.TemporaryDirectory() as tmpdir:
+            mem_file = Path(tmpdir) / "mem.json"
+            subprocess.run([
+                sys.executable,
+                "-m",
+                "GenesisAeonAdvancedAi.aeon_cli",
+                "-1",
+                "--memory",
+                str(mem_file),
+            ], check=True)
+            subprocess.run([
+                sys.executable,
+                "-m",
+                "GenesisAeonAdvancedAi.aeon_cli",
+                "2",
+                "--memory",
+                str(mem_file),
+            ], check=True)
+            result = subprocess.run([
+                sys.executable,
+                "-m",
+                "GenesisAeonAdvancedAi.aeon_cli",
+                "--trend-key",
+                "crep_score",
+                "--memory",
+                str(mem_file),
+            ], capture_output=True, text=True, check=True)
+            data = json.loads(result.stdout)
+            self.assertIn("trend", data)
+
diff --git a/GenesisAeonAdvancedAi/test_memory_store.py b/GenesisAeonAdvancedAi/test_memory_store.py
index 7581dd5..44cd146 100644
--- a/GenesisAeonAdvancedAi/test_memory_store.py
+++ b/GenesisAeonAdvancedAi/test_memory_store.py
@@ -8,6 +8,7 @@ from GenesisAeonAdvancedAi.memory_store import (
     summarize_memory,
     summarize_entries,
     tail_results,
+    trend_metric,
 )
 
 
@@ -67,6 +68,16 @@ class TestMemoryStore(unittest.TestCase):
         self.assertEqual(tail[1]["idx"], 4)
         path.unlink()
 
+    def test_trend_metric(self):
+        path = pathlib.Path("temp_mem.json")
+        if path.exists():
+            path.unlink()
+        store_result({"score": -1}, path)
+        store_result({"score": 1}, path)
+        trend = trend_metric(path, "score", 2)
+        self.assertAlmostEqual(trend, 2.0)
+        path.unlink()
+
 
 if __name__ == '__main__':
     unittest.main()
