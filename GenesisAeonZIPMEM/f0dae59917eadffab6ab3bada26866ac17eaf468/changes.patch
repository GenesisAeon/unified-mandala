commit f0dae59917eadffab6ab3bada26866ac17eaf468
Author: Codex <codex@openai.com>
Date:   Wed Jun 25 20:39:37 2025 +0000

    Add start sigil loader and CLI flag

diff --git a/GenesisAeonAdvancedAi/aeon_cli.py b/GenesisAeonAdvancedAi/aeon_cli.py
index 851cca1..bf918b8 100644
--- a/GenesisAeonAdvancedAi/aeon_cli.py
+++ b/GenesisAeonAdvancedAi/aeon_cli.py
@@ -12,7 +12,7 @@ except Exception:  # pragma: no cover - optional dependency
 from .aeon_processor import fraktal_feedback, fraktal_feedback_metrics
 from .performance_monitor import monitor_performance
 from .memory_store import store_result, load_results
-from .sigil_loader import load_sigil
+from .sigil_loader import load_sigil, load_start_sigil
 from .trikaya import trikaya_state
 
 
@@ -53,6 +53,11 @@ def main(argv: List[str] | None = None) -> None:
         type=Path,
         help="Path to a sigil JSON file to include in the output",
     )
+    parser.add_argument(
+        "--start-sigil",
+        action="store_true",
+        help="Include the packaged StartSigil.json in the output",
+    )
     parser.add_argument(
         "--perf",
         action="store_true",
@@ -81,6 +86,8 @@ def main(argv: List[str] | None = None) -> None:
     sigil_data = None
     if args.sigil:
         sigil_data = load_sigil(args.sigil)
+    elif args.start_sigil:
+        sigil_data = load_start_sigil()
 
     if args.perf:
         result = monitor_performance(values, depth=args.depth)
diff --git a/GenesisAeonAdvancedAi/sigil_loader.py b/GenesisAeonAdvancedAi/sigil_loader.py
index ec9f217..bfd8338 100644
--- a/GenesisAeonAdvancedAi/sigil_loader.py
+++ b/GenesisAeonAdvancedAi/sigil_loader.py
@@ -1,6 +1,7 @@
 from __future__ import annotations
 
 import json
+from importlib import resources
 from pathlib import Path
 from typing import Any, Dict
 
@@ -21,3 +22,16 @@ def load_sigil(path: Path) -> Dict[str, Any]:
         return json.loads(text)
     except json.JSONDecodeError as e:
         raise ValueError(f"Invalid sigil JSON: {e}")
+
+
+def load_start_sigil() -> Dict[str, Any]:
+    """Load the packaged ``StartSigil.json`` file.
+
+    This helper allows tools like :mod:`~GenesisAeonAdvancedAi.aeon_cli` to
+    easily include the project\'s default sigil without requiring an explicit
+    file path.
+    """
+    with resources.files(__package__).joinpath("StartSigil.json").open(
+        "r", encoding="utf-8"
+    ) as f:
+        return json.load(f)
diff --git a/GenesisAeonAdvancedAi/test_cli.py b/GenesisAeonAdvancedAi/test_cli.py
index 735bc1b..b798f26 100644
--- a/GenesisAeonAdvancedAi/test_cli.py
+++ b/GenesisAeonAdvancedAi/test_cli.py
@@ -41,6 +41,19 @@ class TestAeonCLI(unittest.TestCase):
             data = json.loads(result.stdout)
             self.assertIn("sigil", data)
 
+    def test_start_sigil_flag(self):
+        with tempfile.TemporaryDirectory() as tmpdir:
+            input_path = Path(tmpdir) / "vals.txt"
+            input_path.write_text("0.1")
+            result = subprocess.run(
+                [sys.executable, "-m", "GenesisAeonAdvancedAi.aeon_cli", "--input", str(input_path), "--start-sigil"],
+                capture_output=True,
+                text=True,
+                check=True,
+            )
+            data = json.loads(result.stdout)
+            self.assertIn("sigil", data)
+
     @unittest.skipUnless(HAS_YAML, "PyYAML not installed")
     def test_yaml_output(self):
         with tempfile.TemporaryDirectory() as tmpdir:
diff --git a/GenesisAeonAdvancedAi/test_sigil_loader.py b/GenesisAeonAdvancedAi/test_sigil_loader.py
index cf8e946..f7ab0c8 100644
--- a/GenesisAeonAdvancedAi/test_sigil_loader.py
+++ b/GenesisAeonAdvancedAi/test_sigil_loader.py
@@ -3,7 +3,7 @@ import tempfile
 import unittest
 from pathlib import Path
 
-from GenesisAeonAdvancedAi.sigil_loader import load_sigil
+from GenesisAeonAdvancedAi.sigil_loader import load_sigil, load_start_sigil
 
 
 class TestSigilLoader(unittest.TestCase):
@@ -25,6 +25,10 @@ class TestSigilLoader(unittest.TestCase):
             with self.assertRaises(ValueError):
                 load_sigil(path)
 
+    def test_load_start_sigil(self):
+        data = load_start_sigil()
+        self.assertIn("sigillin", data)
+
 
 if __name__ == "__main__":
     unittest.main()
