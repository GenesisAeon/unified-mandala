export async function generateOverlay(data: unknown): Promise<string> {
  // placeholder for GAN-based overlay generation
  console.log('generating overlay with data:', data);
  return 'overlay.png';
}
