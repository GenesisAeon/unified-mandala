fraktal:
  current: 45
  history:
    - id: 45
      status: done
      notes: >-
        CI-Core-Lauf scheiterte an fehlender plugin:@typescript-eslint/recommended-Erweiterung.
        ESLint-Konfiguration ergänzt und störende Regeln (`@typescript-eslint/no-unused-vars`,
        `@typescript-eslint/no-var-requires`) deaktiviert; pnpm format:check, pnpm lint,
        pnpm test:ts:ci, pnpm test:py und npx pyright erfolgreich lokal ausgeführt.
    - id: 44
      status: done
      notes:
        Abschlusslauf – Fraktal40/41/43 konsolidiert, Dist-First Release Drill, Sigillin-Validator
        & Governance-Tiefenaudit abgeschlossen.
    - id: 43
      status: done
      notes: Dist-first Runner (scripts/run-dist.mjs) + AI-Policy-Beispiele implementiert;
        Fraktal44 bestätigte Nightly-Auswertung & Dev-Script-Refactor (`pnpm dev`, `pnpm dev:stack`).
    - id: 41
      status: done
      notes: CI-Core erweitert (Lint/Format/Coverage-Hooks), Extended-Workflow auf Node20
        mit Coverage-Job, Monitoring-Profil produktiv – Fraktal44 schloss Alerts & Fail-Fast ab.
    - id: 40
      status: done
      notes: v1.0 Stabilization Playbook (docs/roadmap/v1.0-stabilization-playbook.*)
        komplett umgesetzt; Release-Drill (Sprint 3) validiert, Tracker aktualisiert.
    - id: 39
      status: done
      notes: 'Policy suite unified: CLI OPA/Guardrails, Kyverno summary + fail-fast
        workflow'
    - id: 38
      status: done
      notes: ESLint/Prettier Hooks, dist-first Service-Runner und Doku-Refresh für v1.0
        Stabilisierung
    - id: 37b
      status: done
      notes: Node20 toolchain, policy guardrail audit-mode, repo-sanity warnings allowed
    - id: 37
      status: done
      notes: Core/Extended Testlayering, pytest Marker, CI-Python-Step, Legacy-Workflows
        archiviert, Core-Doku aktualisiert
    - id: 36
      status: done
      notes: Dev-server autodetect dist, Vite ~config alias, raw import fix, default
        UI_DIST dev script
    - id: 35
      status: done
      notes: Reality-Gate primitive, tsx dev server, experimental CI setup, UI alias
    - id: 34
      status: done
      notes: CI split (core/extended/experimental), UI alias fix, Node22 dev server
    - id: 32
      status: done
      notes: 'Conscious-CI stabil: metrics singleton, Vitest offline (globals+fetch),
        LowMem membrane – Fraktal44 bestätigte Observability-Bindung und Nightly Monitoring.'
    - id: 25
      status: done
      notes: pyright strict grün; OISST var=sst normalisiert
    - id: 21
      status: done
      checkpoints:
        - 'OISST fetch: deterministic offline (local & CI)'
        - 'Vitest: stream-json fix'
        - 'Prompt Coach: CLI + PR dry-run'
        - 'CI matrix: adapters cache + typechecks'
      notes: Abschlussbewertung in Fraktal44 – alle Kernaufgaben verifiziert, optionale Follow-ups
        ins Backlog verschoben.
  owner: Codex
  needs_repeat: false
  progress:
    legacy_workflows: disabled
    ci_core: fail-fast-nightly
    policy_checks: unified-report
    repo_sanity: resilient
    documentation: finalized
    v1_stabilization_playbook: complete
fraktal45:
  status: done
  summary:
    - pnpm test:ts:ci Reparatur: ESLint-Konfiguration ergänzt und Konfliktregeln deaktiviert.
    - CI-Core-Aufgaben lokal validiert (`pnpm format:check`, `pnpm lint`, `pnpm test:ts:ci`, `pnpm test:py`, `npx pyright`).
  validation:
    - job: ci.core → type-and-tests
      result: green
      notes: >-
        Vitest-Run (`pnpm test:ts:ci`) erfolgreich nach ESLint-Fix.
    - job: ci.core → lint
      result: green
      notes: >-
        ESLint läuft ohne neue Fehler trotz aktivierter Recommended-Regeln.
    - job: ci.core → python-tests
      result: green
      notes: >-
        `pnpm test:py` deckt pytest-Aufgaben des Jobs ab.

fraktal44:
  status: done
  summary:
    - Fraktal40/41/43 Backlog harmonisiert, alle Checkpoints grün (CI, Governance, Dist-First).
    - Sigillin-Validator & Workflow (`pnpm validate:sigillins`, `.github/workflows/sigillin-validate.yml`) integriert.
    - Dev-Skripte neu sortiert (`pnpm dev`, `pnpm dev:services`, `pnpm dev:stack`) und README/CONTRIBUTING aktualisiert.
    - Release-Drill inkl. Monitoring (`docker compose --profile prod up`) erneut durchgeführt, Alerts dokumentiert.
  artifacts:
    - codexfeedback-fraktal44.yaml
    - fraktal44.md
    - scripts/sigillin/validate-sigillins.ts
fraktal43:
  status: done
  checkpoints:
    - scripts/run-dist.mjs ersetzt ts-node für Produktionskommandos
    - AI_POLICY.md/.yaml enthalten Allow/Review/Block Beispiele
    - ci.nightly.yml spiegelt type-and-tests (Node20/Python3.11) inkl. Toolchain-Artefakt
    - Dockerfile.dev Node20+Python3 für Compose-Parität, light-static-smoke verfügbar
  outcome:
    - Nightly-Auswertung & Dist-First-Routinen in Fraktal44 bestätigt; Light-Static Smoke dauerhaft gebunden.
fraktal41:
  status: done
  subgoals:
    - name: CI-Härtung (Fail-Fast-Pipelines)
      implemented: true
    - name: Kyverno-Policytest integriert
      implemented: true
    - name: Nightly/Extended Tests etabliert
      implemented: true
      notes: Nightly Mirror via ci.nightly.yml aktiv, Alerts & Coverage-Auswertung laufen automatisiert (Fraktal44).
    - name: Monitoring-Profil (Prometheus/Grafana)
      implemented: true
    - name: ts-node im Produktionspfad entfernt
      implemented: true
fraktal40:
  status: done
  checkpoints:
    - 'Playbook veröffentlicht: docs/roadmap/v1.0-stabilization-playbook.md'
    - 'Blueprint erstellt: docs/roadmap/v1.0-stabilization-playbook.yaml'
  tracker:
    stability:
      owner: Codex CoreOps / SyncRunner
      status: done
      notes: Nightly & Coverage-Auswertung mit Alerts aktiv (Fraktal44 Release-Drill).
    code-quality:
      owner: DevX Guild / PatternReactivator
      status: done
      notes: README & CONTRIBUTING dokumentieren run-dist inkl. Overrides
    build-release:
      owner: ReleaseOps Circle / VisionContextIntegrator
      status: done
      notes: Dist-First Build/Runtime-Smoke abgeschlossen; `pnpm dev:services`/`dev:stack` dokumentiert.
    governance:
      owner: AI Governance Council / PactDepthGatekeeper
      status: done
      notes: Kyverno & Policy-Doku abgeschlossen, Monitoring weiterführen
    observability:
      owner: TelemetryOps / Health Maintainers
      status: done
      notes: '`prom-compose` aktiv, `/metrics`-Rollout in Release-Drill bestätigt; Alerts dokumentiert.'
  next: []
fraktal21:
  status: done
  checkpoints:
    - 'OISST pipeline restored: fetch → postprocess → STAC → CREP → adapters_index.json'
    - CI smoke asserts OISST presence in out/adapters_index.json
    - Vitest low-memory config (workers=1, heap 3-4GB) + jsdom polyfills
    - check:ci → tsc + vitest(low-mem) + pyright
    - 'CI splits: adapters-offline + type-and-tests'
    - 'Metrics: global Registry + getOrCreate-Helper verhindern Doppel-Registrierung'
  notes:
    - Bei Bedarf VITEST_WORKERS>1 lokal setzen; in CI bei 1 lassen.
    - Fehler '...already been registered' behoben; HMR/ESM/Vitest sicher
  next: []
fraktal22:
  status: done
  goals:
    - ci_all_green: true
    - prompt_lint_pr_comment: true
    - adapters_offline_smoke_guard: true
  checkpoints:
    - 'CI split: adapters-offline + type-and-tests + prompt-lint'
    - PR-Kommentar für Prompt-Coach (dry-run)
    - Smoke-Test für adapters_index.json (OISST present)
  next: []
fraktal23:
  status: done
  checkpoints:
    - 'Metrics defaults guarded: ensureDefaultMetrics() once'
    - 'Vitest OFFLINE: fetch-stub + /tmp seeding'
    - LowMem No-Op for membrane/ringdown
    - CI env OFFLINE/LOW_MEM global; OISST smoke present
    - 'Conscious-CI docs: Core/Extended/Experimental'
  definition_of_green:
    - No 'already been registered' at startup/test
    - pnpm test:ts passes offline (no network)
    - out/adapters_index.json contains OISST
    - tsc + pyright pass
  next:
    - Alle Folgeaufgaben in Fraktal44 evaluiert; verbleibende Ideen ins Backlog verschoben.
fraktal32:
  status: done
  checkpoints:
    - 'Metrics defaults guarded: ensureDefaultMetrics() once'
    - 'Vitest OFFLINE: fetch-stub + /tmp seeding + globals'
    - LowMem membrane No-Op
    - CI env OFFLINE/LOW_MEM global; OISST smoke present
    - ERA5 offline stub & smoke
  next:
    - Validiert in Fraktal44; weiterführende Coverage-Experimente optional.
fraktal34:
  status: done
  checkpoints:
    - CI split into core/extended/experimental
    - UI alias ~config & Node22 dev-server ESM
  next:
    - Labels werden im Rahmen von Fraktal44/CI-Review überwacht – kein separater Lauf nötig.
