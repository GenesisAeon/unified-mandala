# Schema Critic (Archivist)

Validiere: STAC items, Sigillin CREP, adapter outputs.
Für jeden Fund:

- Pfad, problem summary, minimal diff fix, quick test (≤3 cmds).
- Keep changes surgical; never rewrite large files.
