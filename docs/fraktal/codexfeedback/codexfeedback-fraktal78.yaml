fraktal: 78
status: done
owner: ChefDevAI
summary:
  - 'ci.core.yml erweitert das Pflichtgate: `pnpm test:unit:coverage` ersetzt den reinen Unit-Lauf,
    lädt das Artefakt `coverage-vitest` hoch und führt danach `pnpm schema:validate`,
    `pnpm maps:validate`, `pnpm sanity` sowie `pnpm policy:check` fail-fast aus; das
    Advisory-Label `ci:advisory` ruft nun `pnpm check:ci` als Soft-Fail-Bundle.'
  - 'Dokumentation aktualisiert (`docs/START-HERE.md`, Workflow-Cheat-Sheet, Stabilization-Playbook
    MD/YAML) und MandalaMap.* / JSON spiegeln Fraktal78 mit neuem Coverage/Policy-Gate;
    codexfeedback* und MandalaMap-Meta auf Fraktal78 gehoben.'
  - 'analysis/devtalk78-evaluation.md bewertet DevTalk-Prioritäten (CI/Governance) und bestätigt
    Umsetzung der Coverage- und Policy-Gates auf Core-Läufe.'
deliverables:
  - .github/workflows/ci.core.yml
  - analysis/devtalk78-evaluation.md
  - codexfeedback-fraktal78.yaml
  - codexfeedback.md
  - codexfeedback.json
  - codexfeedback.yaml
  - docs/START-HERE.md
  - docs/cheatsheets/unified-mandala-workflows.md
  - docs/roadmap/v1.0-stabilization-playbook.md
  - docs/roadmap/v1.0-stabilization-playbook.yaml
  - MandalaMap.md
  - MandalaMap.yaml
  - MandalaMap.json
commands:
  - pnpm check:ci
hook:
  progress: Core-Gates laufen jetzt mit Coverage + Schema/Map/Sanity/Policy fail-fast;
    Dokumentation und MandalaMap spiegeln Fraktal78.
  next_step: Monitoring der Laufzeiten & ggf. Aufteilung in kombiniertes Setup (einmalige
    pnpm install) evaluieren, um redundante Installationen im type_and_tests-Job zu reduzieren.
repeat_required: false
