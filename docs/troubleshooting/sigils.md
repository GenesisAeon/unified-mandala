# Sigils Troubleshooting

- Run `pnpm sigils:index:strict` to validate sigillin files.
- Errors are written to `out/sigils_errors.json`.
- For STAC issues, inspect `out/stac_validation_errors.json` after `pnpm stac:validate`.
