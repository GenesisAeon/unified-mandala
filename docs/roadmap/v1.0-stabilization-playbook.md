# Unified Mandala v1.0 Stabilization Playbook · Fraktal40

Dieses Playbook übersetzt die Ergebnisse des Fraktal40-Entwicklungsgesprächs in konkrete technische Arbeitspakete. Es ergänzt den Konsolidierungsfahrplan aus Fraktal37 und liefert für das Kernteam eine umsetzbare Reihenfolge für die v1.0-Stabilisierung von **unified-mandala**.

## Ausgangslage & Repository-Baseline

- Unified-Mandala ist ein PNPM-Monorepo mit `apps/`, `packages/` und weitreichenden Skripten im Projektstamm (`package.json`). Die Core-Build-Pipeline nutzt `pnpm typecheck`, `pnpm test:unit`, `pnpm test:unit:coverage` sowie `pnpm test:py` innerhalb von `.github/workflows/ci.core.yml` (Node 20, Offline-/Low-Mem-Flags) und bündelt Governance-Gates via `pnpm schema:validate`, `pnpm maps:validate`, `pnpm repomap:*`, `pnpm sanity` und `pnpm policy:check`.【F:package.json†L12-L111】【F:.github/workflows/ci.core.yml†L1-L33】
- Das Experimental-Workflow (`ci.experimental.yml`) führt aktuell Guardrails- und Agenten-Dry-Runs nur als `|| true`-Checks aus, wodurch Fehlermeldungen nicht blockierend wirken.【F:.github/workflows/ci.experimental.yml†L6-L25】
- Die Policy-Suite bündelt derzeit OPA und Guardrails, erzeugt die Ergebnisse jedoch nur als JSON und ohne Kyverno-Einbindung.【F:scripts/policy-suite.mjs†L13-L103】
- Docker-Artefakte nutzen gemischte Node-Baselines (`Dockerfile` → Node 20 Alpine, `Dockerfile.dev` → Node 18 Bullseye) und `docker-compose.yml` startet zahlreiche Services im Dev-Modus mit `pnpm dev` oder `ts-node`-Kommandos.【F:Dockerfile†L1-L11】【F:Dockerfile.dev†L1-L19】【F:docker-compose.yml†L1-L56】【F:package.json†L26-L122】
- Prometheus/Grafana-Artefakte existieren (`observability/`), werden aber noch nicht in einen automatisierten Release-Check eingebunden.【F:observability/prometheus.yml†L1-L7】

## Verantwortlichkeiten · Fraktal43 Sync

| Stream        | Owner (Lead)                        | Backup / Tandem                  | Notizen                                                                                |
| ------------- | ----------------------------------- | -------------------------------- | -------------------------------------------------------------------------------------- |
| stability     | Codex CoreOps / CodexAuditAgent     | Nightly Stewardship (SyncRunner) | Nightly CI spiegelt Core-Gates, Alerts & Coverage-Auswertung via Fraktal44 grün.       |
| code-quality  | DevX Guild                          | PatternReactivator               | README & CONTRIBUTING dokumentieren Dist-First-Regeln, `run-dist` & neue Dev-Skripte.  |
| build-release | ReleaseOps Circle                   | VisionContextIntegrator          | Compose-/Docker-Profile produktiv, Dist-First Release-Drill (Fraktal44) abgeschlossen. |
| governance    | AI Governance Council               | PactDepthGatekeeper              | Policy-Suite konsolidiert, Docs laufen über Governance-Review.                         |
| observability | TelemetryOps / GenesisAeonNavigator | Health Package Maintainers       | Monitoring-Profil aktiv, `/metrics`-Rollout & Alerts in Fraktal44 geprüft.             |

## 1. Stabilität der Kern-Builds

1.  **Core-Gates härten**

- Sicherstellen, dass `ci.core.yml` bei Fehlern in `pnpm typecheck`, `pnpm test:unit`, `pnpm test:unit:coverage` oder `pnpm test:py` sofort abbricht; Nightly-Workflow mit identischer Matrix vorbereiten.
- Node-/PNPM-Versionen ausgeben (`Debug toolchain versions`) und als Build-Health-Artefakt veröffentlichen.
- Nightly-Mirror `ci.nightly.yml` repliziert den Core-Lauf (Node 20/Python 3.11) und lädt Toolchain-Artefakte pro Matrix-Kombination hoch.
- _Status 2025-09-23:_ Format/Lint sind Teil des Core-Jobs, Toolchain-Logging + Artefakt aktiv; Nightly-Matrix spiegelt Core, Auswertung der Ergebnisse folgt.
- _Status 2025-10-01 (Fraktal44):_ Alert-Auswertung + Coverage-Reports automatisiert, Nightly Mirror dokumentiert in codexfeedback.

- _Status 2025-10-20 (Fraktal57):_ JetStream-Bus-Test läuft unter Vitest, CI-Core startet einen Docker-basierten NATS-Server, ruft `pnpm nats:doctor` und `pnpm test:jetstream` als Teil des type-and-tests-Gates auf; `pnpm nats:doctor` liefert detaillierte Hinweise (fehlendes `-js`, Timeout/Proxy, Berechtigungen) und fällt auf `$JS.API.INFO` zurück. Lokale Workflows verwenden `pnpm nats:docker` für JetStream-Container, `pnpm dev:stack` prüft Ports, räumt sie standardmäßig via `kill-port` auf (Opt-out: `UM_DEV_SERVICES_AUTOFREE_PORTS=0`) und verweist bei Bedarf auf `pnpm dev:ports:free`. Runbook `docs/runbooks/nats-jetstream.md` und README spiegeln die erweiterten Troubleshooting-Schritte.
- _Status 2025-10-27:_ Core-, Extended- und Nightly-Jobs installieren `src/adapters/requirements.txt`, sodass NetCDF4/h5netcdf/scipy offline verfügbar sind; `PANTHEON_DISABLE=1` hält QA-Analytics in CI deaktiviert und Coverage-Jobs greifen auf `pnpm test:unit:coverage` zurück.
- _Status 2025-10-29:_ CI-Core nutzt die Skript-Aliase `pnpm typecheck`, `pnpm test:unit` und `pnpm test:unit:coverage`; Governance-Workflow ruft `pnpm schema:validate`, `pnpm maps:validate`, `pnpm policy:check` auf und `pnpm sanity` prüft die neuen `ciBehaviour`-Felder.
- _Status 2025-11-09 (Fraktal73):_ Windows-Setup (`scripts/setup-dev-env.ps1`) erkennt Docker-basierte `nats`-Container, exportiert Status/Start-Hinweise im InstallReport und die NATS-Runbooks dokumentieren `docker ps -a ...`, `docker start nats`, `docker logs --tail=50 nats` sowie den Windows-Portcheck `Test-NetConnection 127.0.0.1 -Port 4222`.
- _Status 2025-11-17 (Fraktal77):_ Workflow `status-gate.yml` prüft das Label **ready-to-merge** auf Draft-Status, ReviewDecision, Mergeability und Status-Rollup; bei Erfolg aktiviert er Auto-Merge bzw. die Merge Queue und entfernt das Label automatisch, falls ein Gate scheitert.
- _Status 2025-11-18 (Fraktal77):_ Provenance-Automation aktiviert – `auto-provenance.yml` vergibt `source:*` Labels (human, human-docs, mandala-ai, external-ai) und `provenance-gate.yml` prüft Pfadmatrix & geschützte Bereiche (packages/\*\*/core, Guard-Skripte, Deploy/Infra). Das neue `ci.runtime.yml` wird nur über `ci:runtime` Label ausgelöst; `/run repomap` & `/run governance` hängen die passenden Labels an. `docs/START-HERE.md` erklärt Golden Path, Two-Plane und Label-Matrix, `scripts/repo/setup-labels.mjs` + `pnpm labels:setup` legen die Labels via GitHub CLI an.
- _Status 2025-11-19 (Fraktal78):_ `ci.core.yml` ersetzt den einfachen Unit-Lauf durch `pnpm test:unit:coverage`, lädt das Artefakt `coverage-vitest` hoch und führt danach `pnpm schema:validate`, `pnpm maps:validate`, `pnpm sanity` sowie `pnpm policy:check` als verpflichtende Gates aus. `docs/START-HERE.md` und `docs/cheatsheets/unified-mandala-workflows.md` spiegeln den erweiterten Pflichtumfang; die optionale Advisory-Lane (`ci:advisory`) ruft jetzt `pnpm check:ci` als Soft-Fail-Bundle auf.

2. **Extended-/Experimental-Schicht**
   - In `ci.experimental.yml` die `|| true`-Konstrukte durch echte Fehlerpfade ersetzen und Guardrails/Agents-Dry-Run nur noch mit optionalen `continue-on-error`-Strategien versehen, sobald die Checks stabil sind.
   - Dry-Run-Logs als Artefakt beibehalten, zusätzlich Statuskommentare in PRs posten.
   - _Status 2025-09-21:_ Experimental-Jobs laufen auf Node 20 und nutzen `agents:health`; Coverage-Job im Extended-Workflow ergänzt.
   - _Status 2025-10-01 (Fraktal44):_ Guardrail/Agents Checks laufen fail-fast, Nightly Alerts verlinken auf Issues.
3. **Agent-/Service-Workflows**
   - `ci/agent.yml` auf Node 20 heben und `pnpm policy:check` als Sammler für Policy-Checks einsetzen, damit Lambda/Go/Python/Node-Pfade synchron laufen.【F:ci/agent.yml†L1-L23】【F:package.json†L93-L141】

## 2. Codequalität & Technische Schulden

1. **Lint/Format-Disziplin**
   - Husky-Hooks (`.husky/pre-commit`) verwenden bereits `lint-staged`; ergänzen um explizite ESLint/Vitest-Dry-Run-Befehle für kritische Dateien.
   - Richtlinien (z. B. „kein `ts-node` im Produktionspfad“) in `CONTRIBUTING.md` und `README.md` dokumentieren.
   - README & CONTRIBUTING dokumentieren den Dist-First-Runner `scripts/run-dist.mjs`, inklusive `UM_RUN_DIST_*` Overrides und Direktaufrufen.
2. **Dead Code & Skript-Konsolidierung**
   - Scripts aus `package.json` auditieren: Legacy-Kommandos (z. B. `ghost-shell:*`, `export_depth_bundle`) nur noch über kompilierten Output (`node dist/...`) ausführen.
   - Gemeinsames CLI (`scripts/dev-services.mjs`, `scripts/dev-server.ts`) dokumentieren und verwaiste Services in `docker-compose.yml` archivieren, falls nicht mehr aktiv betrieben.
   - _Status 2025-09-23:_ `scripts/run-dist.mjs` ersetzt `ts-node`, Dist-First-Richtlinien sind in README/CONTRIBUTING verankert; weitere CLI-Dokumentation folgt.
   - _Status 2025-10-01 (Fraktal44):_ Dev-Skripte aktualisiert (`pnpm dev`, `pnpm dev:services`, `pnpm dev:stack`); README/CONTRIBUTING & codexfeedback spiegeln Finalstatus.

- _Status 2025-11-26 (Fraktal85):_ `analysis/devtalk84-evaluation.md` inventarisiert `pnpm dev:stack`, `pnpm start:cosmic-web`, `pnpm smoke:live` sowie die `ps:*`-Shortcuts. Folgeaufgabe: Diagnoseskript `pnpm diag:shortcuts` + Troubleshooting-Ergänzungen für `docs/DEV-SHORTCUTS.md` einplanen.
- _Status 2025-11-28 (Fraktal87):_ `analysis/devtalk87-evaluation.md` bestätigt die DevTalk-Checkliste (CI/Governance/Observability) als umgesetzt, markierte offene Aufgaben (Shortcut-Diagnose, Port-Offset-Doku, Health-Aggregator-Badge, Gemini-Sigillin-Link) und hielt den Fraktalrun temporär als in-progress.
- _Status 2025-11-29 (Fraktal87 Update):_ `scripts/diag/shortcuts.mjs` liefert `pnpm diag:shortcuts` (Dev-Stack-Diagnose, Live-Smoke JSON, PowerShell-Check). `docs/DEV-SHORTCUTS.md` erweitert Ports/Offsets + Troubleshooting-Tabelle inkl. Health-Aggregator-Badge; `scripts/smoke/live-smoke.mjs` unterstützt `--json/--soft` für diagnostische Läufe. Offene Punkte wurden für Fraktal88 (Climate-MVP Phase 4, Gemini-Sigillin-Verlinkung, Health-Badge) vorgemerkt.
- _Status 2025-11-30 (Fraktal88 Evaluation):_ `analysis/devtalk88-evaluation.md` bestätigt den grünen Zustand von CI/Governance/Observability/Docs nach dem Qwen-Rollout und verschiebt die verbleibenden Aufgaben offiziell in Fraktal88 (Climate-MVP, Gemini-Link, Health-Badge, optionale Policy-Badges).

## 3. Build- und Release-Härtung

1. **Dist-First Strategie**
   - `pnpm build` erzeugt derzeit TS/JS-Artefakte via `tsconfig.build.json`; nach erfolgreichem Build sollen Produktionskommandos ausschließlich `node dist/...` nutzen, keine `ts-node`/`tsx` Calls.【F:package.json†L12-L111】
   - `Dockerfile.dev` von Node 18 auf Node 20 aktualisieren, damit lokale Compose-Umgebungen und CI identische Toolchains verwenden.【F:Dockerfile.dev†L1-L19】
   - `Dockerfile.dev` installiert zusätzlich Python 3 (inkl. Pip/Venv), damit News-/Climate-Orchestratoren mit identischer Toolchain wie in CI laufen.【F:Dockerfile.dev†L1-L23】
   - _Status 2025-09-24:_ Node20-Basiscontainer mit Go & Python aktiv; Dist-First-Kommandos bleiben über `scripts/run-dist.mjs` gewahrt.
   - _Status 2025-10-01 (Fraktal44):_ Release-Drill erfolgreich (`pnpm build` → `pnpm dev:services`/`start:light`), Compose/Docker-Doku aktualisiert.
2. **Runtime Smoke Tests**
   - `pnpm start:light` (Light Static Server) nach jedem Build ausführen und via Script (`scripts/light-static-server.mjs`) Response-Header (Brotli/Gzip) testen.【F:scripts/light-static-server.mjs†L1-L211】
   - Neues Smoke-Script `scripts/smoke/light-static-smoke.mjs` validiert Status-Code, Content-Encoding, Cache-Control und HTML-Fallback nach `pnpm build:ui`.【F:scripts/smoke/light-static-smoke.mjs†L1-L148】
   - _Status 2025-09-24:_ `pnpm smoke:light-static` nutzt den Light-Server (inkl. Health-Endpoint) für Brotli/Gzip-Checks; `smoke:ttfb` bleibt für Performance-Benchmarks bestehen.
   - _Status 2025-10-01 (Fraktal44):_ Light-Static Smoke Teil von Release-Checklist & CI-Docs; Monitoring-Alerts koppeln auf Smoke-Ergebnisse.
   - `docker-compose.yml` für Produktions-Simulation nutzen (`pnpm build`, `docker compose up --profile prod`), Services mit Health-Endpoints ausstatten.

3. **AI Responses Bridge**
   - Workspace `packages/ai` stellt einen dist-first Wrapper auf die OpenAI Responses API bereit (inklusive `.env.example`, Dev-Skript und optionalem NATS-Worker).
   - `apps/api` ergänzt einen Express-Endpunkt `POST /api/ai/chat`, wahlweise direkt (Responses) oder via `AI_TRANSPORT=nats`.
   - Frontend-Demo `MandalaAIPlayground` verbindet sich mit der Route und dient als manueller Smoke-Test.

- `pnpm dev:stack` startet zusätzlich `apps/api` auf Port 4000; der mandala-ui Vite-Proxy leitet `/api/ai` dorthin (`MANDALA_AI_API_ORIGIN` erlaubt Overrides).
- _Status 2025-11-24 (Fraktal82):_ `scripts/dev-services.mjs` prüft vor dem Start auf fehlende Workspace-Builds (z. B. `@unified-mandala/ai/dist`) und führt automatisch `pnpm -F @unified-mandala/ai build` aus, sofern `UM_DEV_SERVICES_SKIP_PREBUILD` nicht gesetzt ist; Windows-Setup-Skript initialisiert den `nats`-Status konsistent, sodass der Installationsbericht keine undefinierten Variablen mehr meldet.
- _Status 2025-11-24 (Fraktal83):_ Responses-Payload nutzt jetzt `input_text`/`output_text` statt `type: 'text'`; der 400-Fehler "Invalid value: 'text'" ist behoben und MandalaAIPlayground beantwortet Requests wieder (bei gesetztem `OPENAI_API_KEY`).
  - _Status 2025-11-?? (Fraktal75):_ Wrapper, API und Playground fertiggestellt; Command-Catalog/Cheat-Sheet dokumentieren `pnpm -F @unified-mandala/ai dev`, `pnpm -F @unified-mandala/ai nats`, `pnpm -F @unified-mandala/api dev`.

4. **Docker/Compose Integration**
   - `Dockerfile` weiterhin für statische UI-Builds nutzen, aber Multi-Stage-Build um CLI/Agent-Artefakte erweitern.
   - Compose-Profile einführen (`core`, `newsbot`, `climate`) und Default-Profil schlank halten.
   - `docker-compose.yml` nutzt Profile für `core`, `newsbot`, `climate`, `llm`, `global`, `agents` und `monitoring`; Grafana läuft konfliktfrei auf Port 3300.【F:docker-compose.yml†L1-L194】
   - _Status 2025-09-24:_ Standard-Aufruf (`docker compose up`) startet nur `ui`; weitere Services werden per Profil explizit zugeschaltet.

- _Status 2025-10-19 (Fraktal57):_ Adapter-Builds verwenden `scripts/lib/python.mjs`, sodass `pnpm adapter:build:era5` über `scripts/adapter-build-era5.mjs` und die OISST/EFFIS-Skripte auch unter Windows die Projekt-venv nutzen.
- _Status 2025-10-27:_ `_normalise_href` berechnet Asset-HREFs relativ zum STAC-Item-Verzeichnis (Fallback auf `file://`), und die Engine-Präferenz für NetCDF erweitert sich um `scipy`; Root-Requirements pinnen `h5netcdf`, damit Offline-Builds stabil bleiben.
- _Status 2025-11-22 (Fraktal79):_ `pnpm start:cosmic-web` orchestriert NATS, Demo-Artefakte, Dev-Services, Vite und den Live-Ticker in einem Kommando und triggert `pnpm smoke:ui` mit dynamischem Port; mandala-ui bedient `/demo/ai-playground` direkt (Vite-Proxy auf Port 4000 → `apps/api`) und `scripts/ci/no-local-path-leaks.mjs` blockt versehentlich eingecheckte Windows-Pfade bzw. Secrets.
- _Status 2025-11-27 (Fraktal86):_ `scripts/dev-helper.ps1` setzt Service-Ports (share, experiments, rag, flags, api, health, realtime) als absolute Werte je Offset, `config/ports.ts` und `apps/ui/vite.config.ts` interpretieren Umgebungsvariablen als finale Ports und `scripts/dev-services.mjs` prüft Portkonflikte ohne doppelte Offset-Aufschläge.

## 4. Teststrategie & Abdeckung

1. **Unit-Tests für Kernfunktionen**
   - Agenten (`agents/`, `services/ghost-shell/`) und CREP-/Policy-Komponenten (`src/adapters/**`, `policies/`) mit Vitest/Pytest-Minimaltests absichern.
2. **Extended Suites**
   - `pnpm test:ts:extended` und `pnpm test:py:extended` weiterhin offline fahren; Flakes markieren (`pytest -m slow`) und nightly triggern.
3. **Coverage & Reporting**
   - `ci.core` führt `pnpm test:unit:coverage` mit Vitest-Coverage aus und veröffentlicht den `coverage/`-Ordner als Artefakt (`coverage-vitest`).

- _Status 2025-10-27:_ Pytests sichern die STAC-HREF-Normalisierung mit Item-Verzeichnis-Referenz, sodass Regressionen wie Fraktal64 früh erkannt werden.

## 5. Governance & Policy Checks

1. **Policy-Suite bündeln**
   - `scripts/policy-suite.mjs` um Kyverno CLI (`pnpm kyverno:validate`) erweitern und einheitliches JSON/Markdown-Reporting in `out/policy/` erzeugen.【F:scripts/policy-suite.mjs†L13-L141】【F:tools/kyverno-dry-run.mjs†L1-L200】
   - Ergebnisse als GitHub Check zusammenführen (OPA, Guardrails, Kyverno) und in `policy:check` Script einbinden.【F:package.json†L109-L146】【F:.github/workflows/ci.experimental.yml†L1-L56】
   - _Status 2025-10-24:_ `pnpm policy:check` ruft `pnpm sigillins:report` auf und legt JUnit + Markdown unter `out/policy/sigillins/` ab; Reports landen gemeinsam mit OPA/Guardrails/Kyverno im Policy-Artefakt.
   - _Status 2025-10-25:_ `policy-check.yml` lädt die Sigillin-Governance-Berichte zusätzlich als separates GitHub-Artefakt (`policy-sigillins`).
   - _Status 2025-10-29:_ Governance-Läufe inkludieren `pnpm schema:validate` und `pnpm maps:validate`; Repo-Sanity erzwingt `ciBehaviour`-Metadaten via `pnpm sanity`.
   - _Status 2025-11-19 (Fraktal78):_ Policy-Suite läuft jetzt direkt im `ci.core`-Gate; Schema-/Map-Checks und Repo-Sanity teilen sich denselben Lauf. Das optionale Label `ci:advisory` ruft weiterhin `pnpm check:ci` als Soft-Fail-Bundle auf, um vollständige Protokolle zu sammeln.

- _Status 2025-10-30 (Fraktal66):_ Policy-Suite markiert Kyverno/Sigillin als optionale Schritte (`PANTHEON_DISABLE=1` Default), protokolliert explizite Skip-Warnungen, wenn Kyverno-CLI oder `sigillins:report` nicht vorhanden sind, `pnpm repomap:*` erzeugt Fallback-Artefakte wenn `dist` fehlt und das Cheat Sheet `docs/cheatsheets/unified-mandala-workflows.md` bündelt Dev-/CI-/Governance-Flows.
- _Status 2025-11-03 (Fraktal68):_ `keys/` ist durch Repo-Sanity (Markerprüfung `.gitignore/.gitkeep`) und eine Kyverno-Regel in `policies/kyverno.yaml` abgesichert; unerwartete Dateien schlagen Governance-Gates fehl, die Stub-Roadmap wurde entsprechend aktualisiert.
- _Status 2025-11-16 (Fraktal76):_ Two-Plane-Modell für Agenten umgesetzt – `packages/ai/src/guards/*` erzwingt `repo://` read-only sowie `scratch://`/`data://` als Runtime-Speicher; `ai-commit-guard.yml` blockiert Bot-Pushes und `.ai-scratch/`-Artefakte und das PR-Template fordert eine explizite CODE↔RUNTIME-Plane-Wahl, wodurch Halluzinations-Fehlschichten (Programm vs. Programm+AI vs. Repo) sichtbar werden.

2. **Dokumentation**
   - `AI_POLICY.md` und `AI_POLICY.yaml` mit Beispielen (Allow/Deny) aktualisieren.
   - Erläutern, wie Guardrail-Fehler zu interpretieren sind (`policies/merge-guardrails.yaml`).
   - _Status 2025-09-22:_ Entscheidungsbeispiele für Allow/Review/Block sind in Markdown und YAML dokumentiert.
3. **Sigillin Governance**
   - `pnpm validate:sigillins` nutzt `scripts/validate-sigillins.mjs` + Schema (`mandala-sigillin.schema.json`); CI-Workflow `sigillin-validate` blockt fehlende CREP/Trikāya/NEXT Angaben.
   - Neues Dev-Protokoll `pnpm dev:breath` koppelt `validate:sigillins` und `trikaya:dashboard` an Dateiänderungen (sigils/, apps/ui/, scripts/) und meldet Coverage-Metriken direkt nach jedem Lauf.
   - _Status 2025-10-01 (Fraktal44):_ Validator & Workflow aktiv, Links-Prüfung + Content-Hooks dokumentiert.

## 6. Dokumentation & Onboarding

1. **README / CONTRIBUTING / ONBOARDING**
   - Release-Pipeline (Core vs. Extended vs. Experimental) beschreiben und Dist-First-Vorgaben aufnehmen.
   - Quickstart um `scripts/setup-dev-env.sh` **und** `scripts/setup-dev-env.ps1` ergänzen; Windows-Build (`scripts/run-powershell.mjs`) dokumentieren.
   - Windows-Setup (`scripts/setup-dev-env.ps1`) exportiert den Installationsstatus nach `out/setup/install-state.json`, damit Codexfeedback-Läufe den Zustand referenzieren können.
   - _Status 2025-10-16:_ PowerShell 7 ParserError (Test-CommandExists ohne Klammern) behoben – Installationsresultate werden zu Bool gecastet und `-and`-Checks nutzen `(Test-CommandExists ...)`, DevTalk 55 Fragment geschlossen.
   - _Status 2025-10-17:_ Corepack-Admin-Detection verhindert EPERM-Warnungen, dokumentiert Aktivierungsmodus im InstallReport und ergänzt README/Onboarding/Command-Catalog um NATS-Installationshinweise (winget/Docker) für `pnpm start:all`.

- _Status 2025-10-19:_ README & Tests-Runbook referenzieren `pnpm nats:docker` und `pnpm dev:ports:free`; das Emergence-Scan-Skript fällt auf `docs/sigillin/GENESIS.md` zurück, wenn `SIGILLIN_GENESIS.md` noch nicht angelegt wurde. Quickstart (Windows · PowerShell) dokumentiert `UI_DEV_URL` und JetStream-Setup; `SIGILLIN_GENESIS.md` ist als Placeholder eingecheckt, damit lokale Läufe deterministisch bleiben.
- _Status 2025-10-21:_ `docs/maps/RepoMap.yaml` deklariert `repo`, `docs/maps/ProgramFlow.yaml` enthält ein `meta`-Block – `pnpm maps:validate` läuft dadurch wieder grün; README (Windows · PowerShell) ergänzt Hinweise zu `Get-Content`/`type` und `$env:`-Zuweisungen.
- _Status 2025-11-02/11-10 (Fraktal67/74):_ DevTalk67 ausgewertet; das Workflow-Cheat-Sheet listet `pnpm check:precommit`, `pnpm check:prepush`, `pnpm check:ci`, `pnpm ci:verify`, Monitoring-/Offline-Bundle-Hinweise sowie den Windows-Setup-Pfad (`scripts/setup-dev-env.ps1`). Codexfeedback\* und MandalaMap\* spiegeln den erweiterten Scope.
- _Status 2025-11-07 (Fraktal70):_ README und `docs/demos/cosmic-web.md` dokumentieren die Cosmic-Web-Demo plattformfreundlich, warnen vor direkten HTTP-Requests auf Port 4222, verlinken den Subscriber `scripts/realtime/sub-cosmic.mjs` (`pnpm sub:cosmic`, Standard-Subject `demo.cosmic`) und erwähnen das gezielte Testskript `pnpm test:unit:crep`.
- _Status 2025-11-18 (Fraktal77):_ `docs/START-HERE.md` bündelt Golden Path, Two-Plane-Regeln und Provenance-Labels; verweist auf `pnpm labels:setup` für das Label-Bootstrap-Skript und auf die neuen CI-Lanes (`ci.runtime`, `/run repomap`, `/run governance`).
- _Status 2025-11-08 (Fraktal72):_ README/CONTRIBUTING beschreiben den Pytest-Wrapper `scripts/run-pytest.mjs` (auto `.venv`), empfehlen weiterhin `pnpm exec cypress install`, dokumentieren den Mandala-Erklärungscheck `pnpm sigils:validate:mini` und routen `prompts:coach` über den Dist-First-Runner inklusive Command-Catalog-Updates.

2. **AI Governance Doku**
   - `AI_POLICY.md` mit praktischen Beispielen, `docs/CommunityOnboarding.md` mit Build-Health-Badges versehen.
3. **Build Health Kommunikation**
   - Badge/Issue-Template, das CI-Status (Core/Extended) visualisiert; Nightly-Fehlschläge erzeugen Issues mit Anhängen.

## 7. Observability & Monitoring

- `observability/prometheus.yml` und `observability/grafana/` in Deploy-Pipeline einbinden; Compose-Profile um Prometheus/Grafana ergänzen.【F:observability/prometheus.yml†L1-L7】
- _Status 2025-09-21:_ Compose-Profil `monitoring` steht zur Verfügung (`docker compose --profile monitoring up`).
- _Status 2025-12-04 (Fraktal95):_ SigilMessage Runtime-Gate (Ajv 2020) schützt Publisher, Prometheus zählt `sigil_emitted_total`, JSONL-Snapshots unter `data/logs/sigils/` speisen den RAG-Index. OpsPanel rendert die MembranePill inkl. Schema-Version (`v1.0.0`) und ASCII-Status.
- _Status 2025-12-05 (Fraktal95 Update):_ Boundary-Smoke prüft `eventKey`-Dedupe (fehlende oder doppelte Keys brechen den Smoke), `tests/smoke/boundary-smoke.spec.ts` deckt die Helper ab und das Runbook `docs/runbooks/sigil-publisher.md` beschreibt die Integration weiterer Bridges mit `publishSigilMessage`.
- _Status 2025-12-06 (Fraktal95 Runtime Hardening):_ Boundary-Service erzwingt stabile `eventKey`-Hashes mit TTL-Dedupe-Cache (`409 duplicate_eventKey`), schreibt nur neue Laws in JSONL/Snapshots, exportiert Prometheus-Zähler `boundary_law_deduped_total` & Gauge `boundary_dedupe_store_size` und das Smoke-Skript prüft 409/Metric (Grafana-Kacheln "Dedupes/min" & "Dedupe cache size").
- _Status 2025-12-01 (Fraktal93):_ Membrane Hardening (ENV-konfigurierbar, Prometheus Counter/Histogram, Boundary-Events) aktiv,
  SigilMessage-Vertrag via Ajv-Test abgesichert; UI-Membrane-Pill vorbereitet (Integration folgt über OpsPanel/Playground).
- _Status 2025-12-02 (Fraktal94):_ `scripts/validate-schemas.mjs` prüft `schemas/sigil-message/1-0-0.schema.json` strikt (Ajv 2020 + Fingerprint) inkl. Valid/Invalid-Fixtures (`sigils/samples/`, `schemas/sigil-message/examples/`); Schema-Gate Teil von `pnpm schema:validate`, MandalaMap & Codexfeedback tracken UI/Boundary-Follow-ups.
- _Status 2025-10-01 (Fraktal44):_ Monitoring-Smokes (Prometheus/Grafana) nach Release-Drill geprüft, Alerts dokumentiert.
- Workspace-Package `packages/health` (`@um/health`) zur Bereitstellung eines einheitlichen `/metrics`-Endpoints verwenden (Node-Services, Python/Go via Sidecar).
- Prometheus-Scrapes in Release-Dokumentation beschreiben, Alerts für 5xx/Latency definieren.
- Fraktal72 erweitert den Observability-Drill um `pnpm observability:check`, das Prometheus `/api/v1/targets` und Grafana `/api/health` (Port 3300) nach dem Monitoring-Profil prüft (`PROMETHEUS_REQUIRE_ACTIVE=0` erlaubt leere Zielmengen, `OBSERVABILITY_SKIP_GRAFANA=1` deaktiviert den Grafana-Check).

- Fraktal84: Zentraler Health-Aggregator (`scripts/health-aggregator.ts`, `pnpm dev:health`) fasst `/health`-Endpunkte (api, fourier, sigil-generator, ghost-shell, realtime-hub) auf `http://localhost:3999/health` zusammen; Dev-Shortcuts dokumentiert unter `docs/DEV-SHORTCUTS.md`.

## 8. Mandala Rollout – Qwen Playground Path

- **Phasenplan dokumentiert:** `docs/roadmap/RoleOutCommands.(md|yaml|json)` übersetzt den schrittweisen Reaktivierungsplan
  (Phase 0–6) in konkrete Kommandos mit Status (Hygiene, Core-Stack, Qwen-Playground, Cosmic-Web, Climate-MVP, Observability,
  Showtime). Jeder Phase ist ein Gate zugeordnet (z. B. `pnpm guard:no-dotenv`, `/health`, Vitest-Contracts), sodass Fortschritt
  und ausstehende Aufgaben transparent bleiben.
- **Neue Smokes & Hooks:** `scripts/smoke/qwen-smoke.mjs` liefert `pnpm smoke:qwen` für den End-to-End-Pfad UI → `/api/ai/chat`
  → Qwen (Ollama/vLLM). Der Husky-Hook `pnpm hook:qwen-smoke` (siehe `.husky/pre-push`) triggert den Smoke automatisch, sobald
  `AI_PROVIDER` mit `qwen-` beginnt (Opt-in via `UM_RUN_QWEN_SMOKE=1`).
- **Proxy & Shortcuts:** `apps/api-lite/ollama-proxy.mjs` (`pnpm start:ollama-proxy`) normalisiert `output_text` und stellt `/health`; `scripts/dev-helper.ps1` bündelt `Start-UMOllama`/`Smoke-Qwen` (`pnpm ps:umo`, `pnpm ps:smoke-qwen`). `docs/DEV-SHORTCUTS.md` & RoleOutCommands.\* dokumentieren den Flow.
- **CI-Trigger:** `.github/workflows/ci.qwen-smoke.yml` ergänzt eine Self-Hosted-Lane (`ci:qwen-smoke` Label bzw.
  `workflow_dispatch`). Runner mit Label `qwen` können damit denselben Smoke fahren; Dokumentation verweist auf laufende Services
  (`pnpm dev:stack`, `pnpm dev:health`) und lokale Qwen-Endpunkte.
- **Status Einordnung:** Fraktal87 ist abgeschlossen; Fraktal88 trackt die verbleibenden Schritte (Climate-MVP Phase 4, Gemini-Link, Health-Badge) und referenziert `analysis/devtalk88-evaluation.md` als aktiven Hook.

## 9. Tracking & Kommunikation

- `codexfeedback.(md|json|yaml)` als Fraktal-Tagebuch weiterführen und pro Umsetzungslauf aktualisieren.
- Fraktal84 protokolliert Dev-Helper, Health-Aggregator und `.env`-Guard (`guard:no-dotenv`) als Dev-Flow-Härtung.
- Neue Artefakte für Fraktal44: `codexfeedback-fraktal44.yaml` und `fraktal44.md` spiegeln Abschluss & Hooks.
- Issues mit Labels `#build-health`, `#policy-check`, `#code-quality` anlegen und mit diesem Playbook verlinken.
- Für Nightly-Automatisierung `runlog-archive.yml` und `repo-sanity.yml` monitoren (Fehler → automatisches Issue).

## Empfohlene Umsetzungsetappen

1. **Sprint 0 – Analyse verankern**: Playbook im Team reviewen, Verantwortlichkeiten zuweisen, Issue-Board vorbereiten.
2. **Sprint 1 – Core stabilisieren**: CI-Core-Workflow und Policy-Suite härten, Node-Versionen angleichen, Dist-First-Builds durchsetzen.
3. **Sprint 2 – Extended & Docs**: Extended-/Experimental-CI, Governance-Doku, README/CONTRIBUTING-Updates, Compose-Profiles.
4. **Sprint 3 – Release Drill**: Vollständiger `pnpm build` → `pnpm start:light` → Docker Compose → Prometheus Smoke → Nightly Report.

> **Hook:** Fortschritt bitte nach jedem Sprint in `codexfeedback.*` und `advancedprogress.json` spiegeln. Sobald alle Etappen als "done" markiert sind, kann der Fraktal40-Lauf geschlossen werden.

## Status-Update Fraktal89

- Security: Guarded FS endpoints, API-Key, loopback bind (HOST=127.0.0.1) and rate limits are in place. [done]
- Observability: Lightweight audit logging on proxy requests. [done]
- Runtime smokes: Added FS smoke (pnpm smoke:fs); Qwen smoke green (pnpm smoke:qwen). [done]
- Next: Map DevTalk.txt items into CI/Policy/Obs/Docs checklists and sync MandalaMap.
