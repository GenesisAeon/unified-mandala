fraktal_run:
  id: Fraktal93
  author: Johann
  timestamp_utc: '${NOW}'
  summary: |
    RealMembrane v0.1 lands with windowed z-score detection, hysteresis and debounce.
    KPI membrane bridge now caches instances per metric; sigillin:strict workflow enables opt-in governance gate.
status:
  real_membrane: landed
  kpi_bridge: cached
  sigillin_strict: label_gate_ready
changes:
  - file: src/membrane/real-membrane.ts
    description: 'Implement RealMembrane v0.1 (window stats + hysteresis + debounce)'
  - file: src/kpi/membrane-bridge.ts
    description: 'Cache per-metric membrane instance and reuse sigils'
  - file: schemas/sigil-message.schema.json
    description: 'Define SigilMessage contract schema'
  - file: .github/workflows/sigillin-strict.yml
    description: 'Label-gated validator/fixer workflow'
next_steps:
  - label: Wire sigil-message schema into schema:validate
    action: 'Extend pnpm schema:validate bundle with schemas/sigil-message.schema.json'
    state: open
  - label: Playground membrane badge
    action: 'Expose A/ΔA + severity + sigil in Playground UI using stepOrBypass cache'
    state: todo
  - label: Extend sigillin validator wordlists
    action: 'Add ES/FR tokens before making sigillin:strict mandatory'
    state: todo
hook:
  progress: 0.65
  resume_hint: 'Run vitest boundary suites, wire schema into CI, then surface Playground badge.'
references:
  roadmap_playbook: docs/roadmap/v1.0-stabilization-playbook.md
  mandala_map:
    - MandalaMap.yaml
    - MandalaMap.json
    - MandalaMap.md
  devtalk: DevTalk.txt
repeat_needed: conditional
repeat_reason: 'Re-run only if membrane vitest suites fail or horizon drift appears in smoke data.'
