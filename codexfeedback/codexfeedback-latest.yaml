fraktal: 114
phase: OPA Test Runner Refresh
status: Rego-Tests nutzen valide `with input as …` und der Runner pinnt das lokale OPA-Binary mit Pretty/Verbose-Ausgabe.
next_hook: Optional OPA-Bundle-/Alertmanager-Doku synchronisieren; MandalaMap-Updates nur bei weiteren Policy-Artefakten.
changes:
  - apps/ethics-api/opa/policy_test.rego
  - scripts/opa/run-opa-test.mjs
  - package.json
  - codexfeedback/codexfeedback-latest.md
  - codexfeedback.md
  - codexfeedback.json
  - codexfeedback.yaml
validate:
  - pnpm opa:test
what_changed:
  - Rego-Tests modellieren Eingaben via `object.union` statt unzulässiger `with default`-Syntax.
  - OPA-Test-Runner nutzt spawnSync, bevorzugt `bin/opa` und aktiviert Pretty/Verbose-Ausgabe.
  - Package.json ergänzt `pnpm opa:fmt` und `pnpm opa:lint` mit Windows-tauglichem Exit-Handling.
